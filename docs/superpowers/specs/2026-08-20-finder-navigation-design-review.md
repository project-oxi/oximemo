# 설계 리뷰: Finder형 탐색 UX 재설계

> **날짜:** 2026-08-20
> **대상:** `2026-08-20-finder-navigation-design.md`
> **성격:** 코드 대조 기반 설계 검증 + 개선 설계. 구현 없음.
> **판정:** 구조는 채택. 단 **차단급 결함 4건**을 고치기 전에는 구현 착수 불가. 원본 §2 결정 중 3·5·7·9와 §3.1·§4.2·§5.1·§6.3·§10은 이 문서로 대체된다.

---

## 0. 총평

**옳은 것:** 3면 구조(사이드바=큐레이션 / 메인=위치의 내용물 / 브레드크럼=위치의 단일 진실)와 브라우즈·쿼리 2모드 분리는 정확한 진단이다. `folderFilter` 타입을 유지하고 해석만 바꾸는 접근도 마이그레이션 비용 대비 이득이 크다. 이 골격은 유지한다.

**틀린 것:** 원본은 **현재 코드의 실태를 두 곳에서 잘못 읽었다.**

1. "폴더로 이동…"이 동작한다고 가정했지만 **무동작 + 거짓 성공 토스트**다. → DnD(P3)가 그 위에 쌓이면 드래그도 무동작.
2. "폴더 내 검색은 Future"라 미뤘지만 **이미 구현되어 있다**. → 스펙대로 하면 기능 제거(회귀).

여기에 **파괴적 동작의 복구 경로 부재**(폴더 삭제 → 노트 대량 휴지통행, 앱 내 복원 UI·IPC 없음)와 **폴더 카드를 176px 노트 카드 그리드에 혼합**하는 물리적 불일치가 더해진다.

즉 **구조는 맞고, 접지(接地)가 틀렸다.**

---

## 1. 검증 방법

읽은 코드: `CardGrid.tsx`(전체), `Sidebar.tsx`(트리부), `stores/ui.ts`, `lib/api.ts`, `lib/tauri.ts`(폴백 디스패치), `ContextMenu.tsx`, `crates/oximemo-core/src/{memo.rs,vault.rs,watcher.rs}`, `src-tauri/src/lib.rs`(커맨드 목록).
추론 표시는 `[INFERENCE]`.

---

## 2. 차단급 결함 (B) — 구현 전 반드시 해소

### B1. `onMoveFolder`가 이동을 하지 않는다 (거짓 성공 토스트)

**증거** — `CardGrid.tsx:232-244`:
```ts
const onMoveFolder = (id: string, folder: string) => {
  void updateMemo(id, null, null)          // ← body/favorite 모두 null. 이동 아님
    .then(() => { …invalidate…; setToast(`→ ${folder || t.folder_root}`); })  // ← 성공 토스트
```
`api.ts:190`에 `moveNote()`가 존재하지만 **호출자가 0건**(`grep moveNote` → 정의 1곳뿐). 주석 자체가 "Until that lands"라 인정한다.
**폴백도 스텁** — `tauri.ts:348-350`: `case "move_note": emitBrowser("memos:changed"); return null;` (파일/스토어 변경 없음).

**영향** — 원본 §2 결정 7("전부 기존 `move_note` IPC 재사용")과 §7("카드 폴더로 이동… 유지")은 사실과 다르다. 사용자는 이동했다고 믿고 토스트를 받지만 노트는 그대로다. P3의 DnD 3종이 모두 이 핸들러를 재사용하도록 설계되어 있어, **P3을 완주해도 이동은 여전히 안 된다.**

**개선** — P0(신설)로 승격:
- `onMoveFolder`를 `moveNote(id, folder)` 호출로 교체, 실패 시 `setError`. 성공 시에만 토스트.
- 폴백 `move_note` 실구현: 대상 메모의 `folder` 필드 갱신 + `oximemo:folders:v1`에 대상 폴더 보장 + `memos:changed` emit.
- 회귀 테스트: 우클릭 이동 후 원래 폴더 목록에서 사라지고 대상에 나타남(폴백 스모크).

### B2. 검색 스코프 회귀 — 있는 기능을 없앤다

**증거** — `CardGrid.tsx:143-145`:
```ts
return base.filter((n) => {
  …
  if (folderFilter !== null && folderFilter !== undefined && n.folder !== folderFilter) return false;
```
즉 **현재 검색은 선택된 폴더로 스코프되며, 그 일치는 이미 직속(`n.folder !== folderFilter`)**이다.

**영향** — 원본 §3.1은 "검색 입력 → 항상 쿼리 모드 전환(전역)", §8은 "폴더 내 검색 = Future"로 규정한다. 그대로 구현하면 폴더 내 검색이 **삭제**된다. §10 수용 기준 8("검색 … 회귀 없음")과 정면 모순.

**개선 — 검색은 모드를 바꾸지 않는다(원본 §3.1 규칙 교체):**

| 상태 | 검색 동작 | 브레드크럼 |
|---|---|---|
| 브라우즈(`""` 또는 경로) + 검색어 | **현재 위치 하위 재귀 검색**(스코프 기본) | `볼트 › 작업` + 검색 칩 `이 폴더 ▾` |
| 브라우즈 + 검색어 + 스코프=전체 | 전역 검색 | 동일 경로 + 칩 `전체 ▾` |
| 쿼리 모드 + 검색어 | 전역 검색 | `검색: {q}` |

- 검색 박스 좌측에 **스코프 칩**(`이 폴더 / 전체`) — Finder의 검색 스코프 바(`This Mac / 폴더명`)와 동형. 기본값은 브라우즈 중이면 `이 폴더`.
- 스코프=`이 폴더`는 **재귀**로 한다(하위 폴더 포함). 검색은 "찾기"이므로 직속 제한은 유효하지 않다 — 현재 코드의 직속 제한은 오히려 버그로 취급하고 재귀로 교정.
- 검색 중에는 폴더 카드 섹션을 숨긴다(결과는 노트 리스트).
- 원본 §8의 "폴더 내 검색(Future)" 항목 삭제.

### B3. 쿼리 모드 "새 폴더"의 자기모순

**모순** — 원본 §4.5: "쿼리 모드에서는 … 볼트 루트에 생성하고 **완료 즉시 해당 위치로 이동하지 않는다**." / §5.3: "폴더 카드가 **이름 선택 상태로 등장** → Enter 확정".
쿼리 모드 화면에는 폴더 카드가 없다(원본 §4.4는 노트 카드만 렌더). 따라서 이름 편집 UI가 나타날 DOM이 없고, 폴더는 `새 폴더`라는 기본 이름으로 사용자 눈에 보이지 않는 곳에 생성된다.

**개선** — 위치 없는 모드에서는 위치 기반 생성을 제공하지 않는다:
- 쿼리 모드의 빈 영역 콘텍스트 메뉴와 헤더 `＋` 드롭다운에서 **"새 폴더" 항목을 렌더하지 않는다**(disabled 아님 — 부재).
- 쿼리 모드의 "새 노트"는 현행 유지(루트 생성 + 상세 열림 → 생성 위치가 상세 화면 폴더 칩에 보이므로 사용자가 즉시 옮길 수 있다).

### B4. 폴더 삭제에 복구 경로가 없다

**증거** — 코어에 `Vault::restore_memo`는 있다(`vault.rs:574`). 그러나 `src-tauri/src/lib.rs`에 `restore*` 커맨드 없음, `api.ts`에 복원 함수 없음, 휴지통 목록 UI 없음. 즉 **앱 내에서 휴지통을 볼 수도, 되돌릴 수도 없다.** 보존 기간 경과 시 `purge`가 하드 삭제한다(`general.trash_retention_days`).

**영향** — 원본 §6.3은 폴더 삭제 시 내용 노트 **전부**를 휴지통으로 보내고, Undo는 §8 Future로 위임한다. 노트 200개 폴더를 오조작 삭제하면 사용자는 Finder로 `.trash`를 직접 파야 한다. 확인 대화 하나로 방어하기엔 손실이 크다.

**개선 — 복구를 같은 페이즈에 넣는다(저비용, 코어 함수 이미 존재):**
1. `delete_folder(path) -> Vec<MemoId>`로 시그니처 변경(휴지통으로 옮긴 노트 id 반환).
2. `restore_notes(ids: Vec<MemoId>)` IPC 신설 — 내부는 `restore_memo` 루프.
3. 삭제 후 토스트에 **"실행 취소" 액션 버튼**(현재 `Toast.tsx`는 메시지 전용이므로 액션 슬롯 1개 추가). 타임아웃 내 클릭 → `restore_notes`.
4. 폴더 자체 복원: 노트 복원이 경로를 보존하므로 폴더는 파일 복원과 함께 재생성된다(빈 폴더였다면 `create_folder`로 재생성).
5. 폴백(`tauri.ts`)도 동일 — `deleted_at` 부여 → 복원 시 해제.

---

## 3. 높은 위험 (H)

### H5. 폴더 카드를 176px 노트 그리드에 혼합하면 빈 박스가 된다

**증거** — `CardGrid.tsx:41-44`: `MIN_COL_W = 240`, `CARD_H = 176`, `ROW_H = 188`. 노트 카드는 **본문 프리뷰 카드**다.
Finder에서 폴더 타일과 파일 타일이 같은 크기인 것은 둘 다 프리뷰가 없기 때문이다. 여기서 폴더 카드는 아이콘·이름·개수뿐이므로 176px 중 120px 이상이 공백이 된다. 원본 §4.2의 "노트 카드와 동일 반경·보더·호버, 과도한 차별화 금지"는 이 앱의 카드 물리와 맞지 않는다.

**개선 — 폴더는 "타일"이 아니라 "위치 바"다:**
```text
┌─ 폴더 바 (스크롤러 최상단, wrap flex) ────────────────────┐
│ [📁 회의록 12] [📁 참고자료 4] [📁 초고 0·2] [＋ 새 폴더]  │  ← 각 32px 높이
└──────────────────────────────────────────────────────────┘
┌─ 노트 그리드 (가상화, 기존 그대로) ───────────────────────┐
```
- 폴더 항목: 높이 32px, `--tag-radius`, 아이콘 12px + 이름 + 개수. 노트 카드보다 **가볍게** — Oxi 원칙("무게로 위계")에 부합하고, 스캔 대상(위치)과 읽을 대상(내용)을 구분한다.
- 폴더가 많으면 바가 2–3줄까지 wrap, 그 이상은 `모두 보기(n)` 칩으로 접기(펼침 상태는 위치별 기억 안 함 — 세션 한정).
- **리스트 뷰**에서는 원본대로 폴더 행(아이콘+이름+`{n} 항목`)을 노트 행 위에 둔다. 리스트는 행 높이가 균일해 혼합이 자연스럽다.
- 폴더 바 끝의 `＋ 새 폴더` 인라인 칩이 생성 입구를 하나 더 제공한다(빈 영역 우클릭 발견성 보완).

### H6. 가상화 offset 미해결

**증거** — `useVirtualizer({ count: rowCount, getScrollElement: () => scrollerRef.current, estimateSize: () => ROW_H })`(`CardGrid.tsx:179-184`). 가상 아이템은 스크롤러 기준 절대 오프셋으로 배치된다. 폴더 섹션을 같은 스크롤러 상단에 넣으면 **모든 노트 행이 폴더 섹션 높이만큼 위로 밀려 겹친다.** 원본 §4.2는 "가상화 목록 위에 비가상 행"이라고만 적어 이 문제를 넘겼다.

**개선** — `useVirtualizer({ …, scrollMargin: folderBarH })`. `folderBarH`는 폴더 바 래퍼에 `ResizeObserver`(기존 `scrollerCallbackRef` 패턴 재사용)로 측정해 state로 전달. GridView의 `virtualItem.start` 사용부는 `- virtualizer.options.scrollMargin` 보정 필요. 이 보정은 P1 수용 기준에 명시한다(폴더 바 1줄/3줄에서 스크롤 정렬 확인).

### H7. 브레드크럼 바 폭 예산이 없다

**실측 합산** — 헤더 `h-12`, `gap-3`(12px)×4, 검색 `w-56`(224), 뷰 스위처 4개 라벨 버튼(`px-2.5` + 텍스트 "timeline" 등 ≈ 200), 자물쇠(24+4), `＋`+`HTML` 분할 버튼(28+26), 설정(≈28) → **우측 컨트롤만 약 560–590px**. 창 1000px에서 사이드바(≈220px, `Sidebar.tsx` 고정폭) 제외 시 콘텐츠 780px → **브레드크럼 가용 폭 190–220px**. `볼트 › 작업 › 2026-Q4`(≈180px)에서 이미 한계, 한글 폴더명 2단계면 초과.

**개선 — 우측을 먼저 줄인다:**
1. 뷰 스위처를 **아이콘 4개**(`LayoutGrid / List / Clock / Network`, 각 24px)로 교체 → 200px → 100px. 라벨은 `title`/`aria-label`로 유지.
2. 브레드크럼 오버플로: 가용 폭 초과 시 `루트아이콘 › … › 부모 › 현재`(양끝 우선, 중간 축약). 원본의 "앞쪽부터 축약"보다 방향 감각이 유지된다.
3. 콘텐츠 폭 < 640px: 검색을 아이콘 버튼으로 접고 클릭 시 헤더를 검색 전용 행으로 전환(Escape로 복귀).
4. 자물쇠는 브라우즈 모드에서만 렌더(원본 유지) → 쿼리 모드에서 28px 회수.

### H8. Escape 과적재 — 내비게이션에서 제거

Escape는 이미 상세 다이얼로그·캡처 오버레이 닫기에 쓰이고, 검색 입력 클리어 기대도 있다. 원본 §3.2는 여기에 "한 레벨 위"를 더해 3중 의미로 만든다. Finder에서 상위 이동은 `⌘↑`이고 Escape는 취소다. 위치 이탈은 **취소가 아니라 이동**이므로 Escape에 실으면 오조작 시 맥락을 잃는다.

**개선** — Escape: (1) 다이얼로그/오버레이 닫기 → (2) 검색어 클리어(`localSearch`+`debounced`+`search` 3개 동시 리셋 필요) 까지만. 상위 이동은 `⌘↑` 단독. `⌘↓`는 셀렉션 모델 부재로 미할당(원본과 동일).

### H9. `""`(루트) vs `null`(쿼리) 분리가 기존 뷰 핀 설정을 재해석한다

**증거** — `CardGrid.tsx:107` `setFolderView(folderFilter ?? "", v)`, `295-297` `isLocked`도 `folderFilter ?? ""`. 현재 **"전체 보기"와 "루트"는 같은 `FolderDef{path:""}` 엔트리를 공유**한다. 원본이 두 상태를 분리하면 기존 사용자가 "전체 보기"에 걸어둔 핀이 "루트 브라우즈" 핀으로 바뀐다. 또한 원본은 쿼리 모드에서 자물쇠를 숨기라고만 하고 **쿼리 모드의 뷰 모드를 어디에 저장할지 정의하지 않는다.**

**개선**
- `FolderDef{path:""}`는 **루트 브라우즈 전용**으로 계승(의미가 가장 가깝다). 마이그레이션 코드 불필요, 동작 변화는 문서화만.
- 쿼리 모드 뷰는 폴더 속성이 아니므로 `localStorage["oximemo.queryView"]`에 저장(세션 간 유지, config 오염 없음). 자물쇠는 계속 숨김.
- `useEffect`(CardGrid:97-100)의 핀 동기화는 브라우즈 모드에서만 동작하도록 가드.

### H10. 트리 제거의 대체 조망 수단이 없다

트리는 `depth<1` 자동 펼침 + 개수로 **볼트 전체 구조를 한 화면에** 보여줬다(`Sidebar.tsx:79`). 원본의 대체는 브레드크럼(현재 경로만) + 고정 폴더(수동 큐레이션)뿐이다. 신규 사용자는 고정이 0개라 사이드바 FOLDERS 섹션이 비고, 3단 이상 계층에서 "어디에 뭐가 있나"를 잃는다. 원본 §1.3은 트리 병존을 "중복"으로 기각했지만, 기각의 대가를 메우지 않았다.

**개선 3종 — 전부 저비용, 선례 있음:**
1. **브레드크럼 세그먼트 드롭다운** (Windows 탐색기 표준): 각 세그먼트에 `▾`. 클릭 시 그 레벨의 **형제 폴더 목록** → 임의 점프. `list_folders` 결과를 프리픽스로 필터하면 추가 IPC 0.
2. **"폴더로 이동" 팔레트 `⌘⇧O`**: 전체 폴더 fuzzy 검색 점프(현재 앱에 팔레트 없음 — `⌘K` 미존재 확인). 노트가 아닌 **폴더 전용 점프**로 범위를 좁혀 저비용 유지. `move_note`의 "폴더로 이동…" 서브메뉴(폴더 많으면 사용 불가)도 이 팔레트로 대체 가능.
3. **최초 고정 시드**: 고정이 0개이고 최상위 폴더가 있으면 사이드바 FOLDERS 섹션에 **최상위 폴더 전체를 자동 표시**(암시적 고정, `pinned` 미설정 상태). 사용자가 하나라도 명시적으로 고정/해제하면 그 순간부터 명시 목록만 사용. → 빈 섹션과 "고정을 몰라서 못 쓰는" 문제를 동시 해결.

---

## 4. 중간 (M)

| # | 결함 | 개선 |
|---|---|---|
| M11 | **`note_count`는 직속 카운트**(`vault.rs:118-127`, `rel.rfind('/')`). `작업/2026/x.md`만 있으면 `작업`은 "노트 0" → 빈 폴더로 오독(사이드바 트리도 이미 같은 결함) | 폴더 메타를 `하위 n · 노트 m` 형태로. 직속 0 + 하위 존재면 `폴더 n`만 표시. 재귀 총계가 필요하면 `list_folders`에 `note_count_deep` 추가(BTreeMap 접미사 합산, 저비용) |
| M12 | 헤더가 `data-tauri-drag-region="deep"`(`CardGrid.tsx:373`) — 브레드크럼 버튼 클릭·드롭 타깃이 **창 드래그와 충돌** | 브레드크럼 래퍼에 드래그 예외(`data-tauri-drag-region="false"`) 명시. DnD hover 중에는 예외 유지 확인을 P3 수용 기준에 |
| M13 | `sidebarToggle`이 `fixed left-[82px] top-0`(`CardGrid.tsx:282`) — 브레드크럼 시작 좌표와 겹침 | 브레드크럼 좌측 패딩을 토글 폭 이후로 두거나 토글을 헤더 flex 안으로 편입(후자 권장 — fixed 좌표 하드코딩 제거) |
| M14 | 콘텍스트 메뉴가 Base UI `ContextMenu.Trigger`(우클릭/롱프레스). 키보드 트리거 지원 불명 [INFERENCE: 미지원] → 원본 §2 결정 7의 "키보드 사용자를 위해 서브메뉴"가 성립하지 않을 수 있음 | P1에서 `Shift+F10`/`ContextMenu` 키 동작을 실측. 미지원이면 카드·폴더 항목에 포커스 가능한 `⋯` 오버플로 버튼 추가(같은 메뉴 재사용) |
| M15 | DnD 자동 스크롤을 "확인 후 불가하면 보조"로 미결(§5.1). WKWebView에서 HTML5 DnD 자동 스크롤은 신뢰 불가 [INFERENCE] | 처음부터 포함: 스크롤러 상·하단 48px에서 `dragover` 시 `requestAnimationFrame` 가속 스크롤. 미결 사항으로 남기지 않는다 |
| M16 | 드롭 규칙이 "현재 폴더로 드롭 = no-op"만 정의 | 규칙 명문화: 대상이 노트의 현재 폴더면 무동작(하이라이트도 없음), 조상 폴더면 이동 허용, 자기 자신을 담은 폴더 바 항목은 비활성 표시 |
| M17 | `rename_folder` 부분 실패를 "watcher가 자가 치유"로 처리(§6.2). watcher(`watcher.rs`)는 **인덱스만** 치유한다 — 사용자는 절반 이름이 바뀐 두 폴더를 본다 | 사전 검사(대상 존재·쓰기 권한) 후 실행. 실패 시 에러 토스트에 `N개 노트가 "{from}"에 남아 있습니다` + 재시도 액션. "자가 치유" 표현은 인덱스 한정으로 수정 |
| M18 | 수용 기준 9("폴백에서 1–7 동일 동작")이 달성 불가 — 폴백 `move_note`는 무동작 스텁(`tauri.ts:348`), `graph_data`는 빈 결과(`352`), 검색은 substring(`203`) | B1에서 폴백 `move_note`를 실구현한 뒤, 기준 9의 범위를 "폴더 열거/진입/생성/이름 변경/삭제/이동/고정"으로 한정 명시. 그래프·BM25 랭킹은 제외 |
| M19 | 원본 문장 파손·오타: §3.1 "브라우즈 모드에서 태그/검색을 건조건?", §6.1 "무시하도로" | 원본 패치(§7) |

---

## 5. 개선된 핵심 결정 (원본 §2 대체분)

| # | 원본 | 개선 |
|---|---|---|
| 3 | 브라우즈는 직속만 | 유지. 단 **검색은 예외** — 스코프 `이 폴더`는 재귀(B2) |
| 5 | 브레드크럼 바가 헤더 대체 | 유지 + **폭 예산 규칙**: 뷰 스위처 아이콘화, 양끝 우선 축약, 640px 미만 검색 접기(H7) |
| 7 | 이동 3계층, 기존 `move_note` 재사용 | **먼저 `move_note` 배선을 고친다(P0)**. 그 위에 DnD. 폴백도 실구현(B1) |
| 9 | `rename_folder` 신설 + `delete_folder` 휴지통화 | 유지 + **`restore_notes` IPC와 Undo 토스트를 같은 페이즈에**(B4). `delete_folder`는 이동한 id 목록 반환 |
| 신설 11 | — | **검색은 모드를 전환하지 않는다.** 스코프 칩(`이 폴더/전체`)이 범위를 정한다(B2) |
| 신설 12 | — | **폴더는 타일이 아니라 32px 위치 바.** 가상화는 `scrollMargin`으로 보정(H5·H6) |
| 신설 13 | — | **조망 대체 3종**: 브레드크럼 세그먼트 드롭다운 / `⌘⇧O` 폴더 점프 팔레트 / 최초 고정 시드(H10) |
| 신설 14 | — | **위치 없는 모드에는 위치 기반 생성 없음** — 쿼리 모드에 "새 폴더" 부재(B3) |
| 신설 15 | — | 쿼리 모드 뷰 모드는 `localStorage`, 폴더 뷰 핀은 `FolderDef{path:""}`가 루트 브라우즈를 계승(H9) |

---

## 6. 개선된 구현 페이징

- **P0 배선 교정 (신설, 최우선)** — `onMoveFolder` → `moveNote` 실호출 + 실패 처리 / 폴백 `move_note` 실구현 / 우클릭 이동 회귀 확인. **P0 없이는 P3이 무의미하다.**
- **P1 내비게이션 코어** — `MemoFilter.immediate` + IPC / 브레드크럼 바(세그먼트·오버플로·드롭다운·드래그 예외) / 뷰 스위처 아이콘화 / 폴더 위치 바 + `scrollMargin` 보정 / 리스트 뷰 폴더 행 / 시작값 `""` / 사이드바 재편(트리 제거·고정 시드·스마트 컬렉션 라벨) + `FolderDef.pinned` + `set_folder_pinned` + 폴백 / `⌘↑` / 쿼리 뷰 저장 분리 / M14 키보드 트리거 실측
- **P2 폴더 관리** — 폴더 콘텍스트(열기·이름 변경·고정·삭제) / `rename_folder`(사전 검사·부분 실패 메시지) + 폴백 / `delete_folder` 휴지통화 + `Vec<MemoId>` 반환 + **`restore_notes` + Undo 토스트** + 확인 대화 / 빈 영역 콘텍스트(쿼리 모드에서 새 폴더 부재) / 폴더 바 `＋` 칩 / 설정 고정 토글
- **P3 이동 & 검색 스코프** — DnD 3타깃 + 자동 스크롤 + 드롭 규칙(M16) / 검색 스코프 칩(`이 폴더` 재귀 / `전체`) / 쿼리 모드 폴더 칩
- **P4 조망 & 폴리시** — `⌘⇧O` 폴더 점프 팔레트 / 폴더 메타 `하위 n · 노트 m` / 빈 상태 CTA / ARIA(브레드크럼 `nav`+`aria-current`, 폴더 바 `role="list"`) / Timeline·Graph 재귀 확인 / 폴백 회귀

---

## 7. 원본 문서에 적용할 패치

이 리뷰 채택 시 원본을 다음과 같이 고친다(문서 작업, 구현 아님):

1. 머리말에 `리뷰: 2026-08-20-finder-navigation-design-review.md — §2 결정 3·5·7·9와 §3.1·§4.2·§5.1·§6.3·§10은 리뷰 문서가 대체` 추가.
2. §3.1 파손 문장("…건조건?") → B2의 검색 스코프 표로 교체.
3. §4.2 폴더 카드 → 폴더 위치 바(H5) + `scrollMargin`(H6).
4. §5.1에 자동 스크롤·드롭 규칙 확정(M15·M16).
5. §6.1 오타 "무시하도로" → "무시하도록".
6. §6.3에 `Vec<MemoId>` 반환 + `restore_notes` 추가(B4).
7. §8에서 "폴더 내 검색"·"휴지통 복원 UI(부분)" 제거 — 각각 P3·P2로 승격.
8. §10 수용 기준: 폴백 범위 한정(M18), 이동 검증을 "실제 파일/스토어 위치 변경 확인"으로 강화(B1), 폴더 바 스크롤 정렬 항목 추가(H6).

---

## 8. 최종 판단

| 항목 | 판정 |
|---|---|
| 3면 구조·2모드 분리 | **채택** — 진단과 처방이 정확 |
| 브레드크럼 중심 내비게이션 | **채택** + 폭 예산·세그먼트 드롭다운 보강 |
| 폴더를 메인에 노출 | **채택** but 렌더 형태는 타일 → 32px 위치 바로 교체 |
| 사이드바 트리 제거 | **조건부 채택** — 조망 대체 3종 동반 필수 |
| 이동 3계층 | **선행 수정 필수** — 현재 이동 자체가 무동작 |
| 폴더 삭제 휴지통화 | **조건부 채택** — `restore_notes` + Undo 동반 필수 |
| Escape 내비게이션 | **기각** — `⌘↑`로 대체 |
| 검색의 쿼리 모드 강제 전환 | **기각** — 스코프 칩으로 대체 |
| 쿼리 모드 새 폴더 | **기각** — 항목 부재 |
| 구현 착수 가능 여부 | **P0 + 위 대체안 반영 후 가능** |
