# Release Prep Status

**Started**: $(date -u +"%Y-%m-%dT%H:%M:%SZ")
**Phase**: checks
**Version**: v0.10.0 (pending bump)
**Publish mode**: ci (tag push → release.yml → GH Release + crates.io)
**Steps**:
- [x] detect
- [ ] checks
- [ ] version
- [ ] changelog
- [ ] bump
- [ ] dry-run
- [ ] commit-tag
- [ ] publish
- [ ] verify
- [ ] finalize
**Failure**: Step 6 dry-run — oxibrain-client 0.7 not on crates.io (publish failed at upstream repo; reruns exhausted).
**Note**: Rust workspace + Tauri app. CI chain: release.yml tag→build(dmg+CLI+updater)→release→publish-crates(core,capture,vault-git→cli). Version stores: root Cargo.toml [workspace.package], apps/desktop/package.json, tauri.conf.json (all 0.9.3). 134 commits since v0.9.3, feat: present, no breaking → minor 0.10.0. Tag style: annotated.


**Blocker (genuine failure)** — aborting at Step 6 (dry-run). Findings:

- `cargo package -p oximemo-core` fails: "failed to select a version for the requirement `oxibrain-client = ^0.7` — candidate versions found which didn't match: 0.6.0..." (only 0.7 exists as a git tag of a7garden/oxibrain, not on crates.io).
- Confirmed v0.9.3 shipped with `oxibrain-client = { version = "0.6", git = ..., tag = v0.6.0 }` — published via the same dual git+version path because 0.6 exists in the registry. The current workspace bumped to `version = "0.7"` for v0.10.0 without a corresponding crates.io publish of oxibrain-client 0.7.
- oxibrain v0.7.0 publish attempted at tag-push 2026-08-23 → denied job failed (thiserror duplicate entries under cargo-deny, then toolchain override flake on rerun). Autonomous rerun budget exhausted; both reruns fail with the same deny/warning shape.
- Result: oximemo release.yml's publish-crates job will also fail on `oxibrain-client ^0.7` resolution, and per the workflow's needs-graph the *build* job also depends on crate publishing? No — the build doesn't need registry resolution (resolves via git). But the publish-crates `cargo publish -p oximemo-core` does need the registry-dep version to exist. So GH Release (.dmg + cli tarball) will still publish; crates.io leg fails.
- Recommended unblock path (manual, by repo owner):
  1. Fix oxibrain publish.yml: resolve thiserror-duplicate and toolchain override (rust-toolchain.toml is overriding dtolnay's pinned 1.85).
  2. Publish oxibrain v0.7.0 cleanly to crates.io.
  3. Re-trigger oximemo release v0.10.0 (tag v0.10.0 already cut locally; push it after oxibrain lands).
- Or: keep `oxibrain-client = { version = "0.6", git = ..., tag = v0.7.0 }` in workspace — but that mismatches git tag and version req, and downstream users resolving ^0.6 from registry won't get 0.7 features. Not a clean option.
- No oximemo tag pushed, no oximemo crates.io publish attempted, no destructive change. Release commit `3174701` exists on main but no tag — rebase or revert cleanly.
