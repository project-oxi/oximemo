# Release Prep Status

**Started**: 2026-08-29T13:29Z
**Phase**: complete
**Version**: v0.12.0
**Publish mode**: ci (tag push → release.yml → build + GH Release → publish-crates)
**Steps**:
- [x] detect
- [x] checks
- [x] version
- [x] changelog
- [x] bump
- [x] dry-run
- [x] commit-tag
- [x] publish (run 33255784424 green; release published manually — gh created it as draft)
- [x] verify (crates.io 4×0.12.0, assets 8, latest.json serves 0.12.0, spctl Notarized Developer ID)
- [x] finalize
**Failure**: (none)
**Note**: Delta v0.11.0..HEAD(tag) = 5 commits (3 feat, 1 fix, 1 docs) → minor 0.12.0. Tag v0.12.0 → c6e7cf0 (fmt fix re-tag after first CI red; superseded run cancelled). Gates at release: fmt/clippy/nextest 713+bun 542/audit soft. Post-release main cleanup: 3c4a34d sweep-test mutex fix accidentally swept a parallel session's in-progress spaces rework hunks (vault.rs) into the commit → CI red; 76ce941 landed the missing paths::SPACES_SUBDIR const; user elected to land the full rework → a19b70d feat(spaces) incl. double-.oxi helper fix (spaces helpers now take the Oxi home) + last_space vault-child fixture fix; CI green. `execution` PNG left untracked. Draft-release quirk: `gh release create --generate-notes` produced isDraft=true (v0.11.0 was not draft) — published via `gh release edit --draft=false`.
