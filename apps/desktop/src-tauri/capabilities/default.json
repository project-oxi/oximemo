{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main + capture windows.",
  "windows": ["main", "capture"],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "dialog:default"
  ]
}
