# Pins the Rust toolchain so local and CI (GitHub Actions) run an identical
# compiler + lint set. `dtolnay/rust-toolchain@stable` in CI honors this file.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
